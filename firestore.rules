rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    // Gatepath never reads or writes learner state directly from a browser.
    // The trusted FastAPI service uses Firebase Admin, which bypasses rules.
    match /{document=**} {
      allow read, write: if false;
    }
  }
}
